# Detection runtime. Prefer the target runtime's existing compatible binary wheels.
# Keep NumPy/SciPy below ranges that have caused mixed-ABI issues in Colab when
# pip replaces them underneath an already-running kernel.
torch>=2.2
numpy>=1.26,<2.4
scipy>=1.11,<1.17
pillow>=10,<12
huggingface_hub>=0.30,<2
# RF-DETR 1.8.3 requires transformers>=5.1,<6.
transformers>=5.1,<6
rfdetr==1.8.3
