# Detection runtime. PyTorch CUDA wheels may be provided by the target runtime.
# RF-DETR 1.8.3 requires transformers>=5.1,<6; keep this range aligned with
# the detector package so pip can resolve the Colab environment.
# NumPy 2.4.x currently breaks the SciPy/NumPy import chain used by RF-DETR on
# Colab (missing numpy._core._multiarray_umath._blas_supports_fpe). Keep the
# runtime below 2.4 until that upstream compatibility gap is gone.
torch>=2.2
numpy>=1.26,<2.4
pillow>=10,<12
huggingface_hub>=0.30,<2
transformers>=5.1,<6
rfdetr==1.8.3
